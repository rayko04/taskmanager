info:
  name: PATCH /tasks/:id
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:8080/tasks/1
  body:
    type: json
    data: |-
      {
        "completed": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
