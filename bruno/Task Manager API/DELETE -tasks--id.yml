info:
  name: DELETE /tasks/:id
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/tasks/24
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
